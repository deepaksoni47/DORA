spring.config.import=optional:file:backend/.env[.properties],optional:file:.env[.properties],optional:file:../.env[.properties]

server.port=8080

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/dora_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

youtube.api.key=${YOUTUBE_API_KEY:}
github.api.key=${GITHUB_API_KEY:}
